
// Simulated esptool.min.js (minified for local use in BLOKDBIT Web Flasher)
window.esptool = {
  Transport: class {
    constructor(device) {
      this.device = device;
    }
    async connect() { console.log("Simulated transport connect"); }
    async disconnect() { console.log("Simulated transport disconnect"); }
  },
  ESPLoader: class {
    constructor(transport, chip) {
      this.transport = transport;
      this.chip = chip;
    }
    async initialize() { console.log("Simulated ESP init"); }
    async eraseFlash() { console.log("Simulated erase"); }
    async flashData(data) { console.log("Simulated flash", data); }
  }
};
